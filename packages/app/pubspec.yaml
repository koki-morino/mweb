name: memkept
description: "Memory keeping"
# Do NOT publish.
publish_to: "none"
# Fix optional number on +1.
version: 1.0.0+1

environment:
  sdk: ^3.13.2
resolution: workspace

dependencies:
  bloc: ^9.0.0
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  google_fonts: ^8.2.1
  logging: ^1.2.0
  protobuf: ^6.0.0
  uuid: ^4.5.0
  web_socket_channel: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - path/to/asset/
